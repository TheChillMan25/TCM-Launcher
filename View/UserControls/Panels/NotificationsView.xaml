<UserControl x:Class="TCM_Launcher.View.UserControls.Panels.NotificationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:TCM_Launcher.View.UserControls.Panels"
             Height="400"
             Width="600"
             VerticalAlignment="Center"
             HorizontalAlignment="Center">
    <Border Background="#171717"
            CornerRadius="8"
            BorderThickness="1"
            BorderBrush="#666">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid Name="Header">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Label Content="Notifications"
               Style="{StaticResource WindowTitle}"/>
                <Button x:Name="CloseButton"
                Grid.Column="1"
                Style="{StaticResource RoundButton}"
                Width="30"
                Height="30" Click="CloseButton_Click">
                    <Path Data="{StaticResource CloseIcon}"
                  Fill="#f2c480"
                  Width="15"
                  Height="15"
                  Stretch="Uniform"/>
                </Button>
            </Grid>
            <ScrollViewer HorizontalScrollBarVisibility="Disabled"
                          VerticalScrollBarVisibility="Auto"
                          Grid.Row="1">
                <ItemsControl ItemsSource="{Binding Notifications}"/>
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>
